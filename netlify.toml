[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Forward client API routes securely to Netlify Serverless Functions to hide the GEMINI_API_KEY
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
